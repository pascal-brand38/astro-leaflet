---
// Copyright (c) Pascal Brand
// MIT License
//

import type { AstroLeafletControlType } from '../index';

type Props = AstroLeafletControlType;
---

<!-- dummy component to call the constructor of the custom element astro-leaflet-control,
     which is defined in Leaflet.astro -->
<astro-leaflet-control id={Astro.props.id || ''} data-props={JSON.stringify(Astro.props)}>
  <slot />
</astro-leaflet-control>
